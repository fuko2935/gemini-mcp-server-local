runtime: "typescript"
startCommand:
  type: "stdio"
  configSchema:
    type: "object"
    properties:
      geminiApiKey:
        type: "string"
        description: "Gemini API key for code analysis"
    required:
      - geminiApiKey
env:
  NODE_ENV: "production"
  DEBUG: "false"
  LOG_LEVEL: "info"